# Manuscript audit (retired from UI)

This folder and related engine hooks remain in the repository for future reuse (L3, Marker PDF, LLM grounding). The main application no longer mounts `AuditView` or exposes manuscript mode in menus.
